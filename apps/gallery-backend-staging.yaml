apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gallery-backend-staging
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Yevgenio/boukingolts-cluster
    targetRevision: HEAD
    path: manifests/gallery-backend-staging
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: gallery-backend-staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
