apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: plants-backend-staging
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Yevgenio/plants-cluster
    targetRevision: HEAD
    path: manifests/backend-staging
    helm:
      releaseName: backend
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: plants-staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
