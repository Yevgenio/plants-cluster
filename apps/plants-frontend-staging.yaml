apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: plants-frontend-staging
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Yevgenio/plants-cluster
    targetRevision: HEAD
    path: manifests/frontend-staging
    helm:
      releaseName: frontend
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: plants-staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
