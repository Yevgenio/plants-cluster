apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: argocd
  namespace: argocd
spec:
  entryPoints:
    - websecure
  tls:
    passthrough: true
  routes:
    - match: Host(`argocd.boukingolts.art`)
      kind: Rule
      services:
        - name: argocd-server
          port: 443
