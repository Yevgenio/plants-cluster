replicaCount: 1

image:
  repository: registry.boukingolts.dev/plants-frontend
  # Updated automatically by Argo Workflows on each push to main
  tag: "348d22c"
  pullPolicy: Always

registry:
  enabled: true
  url: "registry.boukingolts.dev"

service:
  type: ClusterIP
  port: 3000

ingress:
  host: boukingolts.dev

env:
  # Server-side only — not baked into the image, overrides NEXT_PUBLIC_API_URL for SSR
  apiUrl: http://backend:5000

resources:
  requests:
    cpu: 50m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi
