apiVersion: v1
kind: ConfigMap
metadata:
  name: gallery-dashboard
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  gallery.json: |
{{ .Files.Get "files/gallery.json" | indent 4 }}
