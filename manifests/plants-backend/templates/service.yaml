apiVersion: v1
kind: Service
metadata:
  name: plants-backend
  namespace: {{ .Release.Namespace }}
  labels:
    app: plants-backend
spec:
  type: {{ .Values.service.type }}
  selector:
    app: plants-backend
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 5000
      protocol: TCP
